{
  "frontend/src/App.tsx": "Main app router using named import `{ LoginPage }` from LoginPage, with all routes wired to their page components under a shared layout route.",
  "frontend/src/contexts/AuthContext.tsx": "Authentication context provider managing Internet Identity login state, session creation/management, and providing authentication status throughout the app.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations, now using `useAuth` from AuthContext (not `useSession`), with both canonical `useGetXxx` names and backward-compat aliases for old names (`useIngredients`, `useCustomers`, `useInvoices`, etc.).",
  "frontend/src/components/Layout.tsx": "Main layout with sticky header, desktop/mobile navigation, logout button, and footer — uses named import `{ NotificationPanel }`.",
  "frontend/src/pages/LoginPage.tsx": "Login page with Internet Identity authentication, uses `isLoggingIn` from AuthContext (not `isLoading`), and exports both named and default export.",
  "frontend/src/pages/DashboardPage.tsx": "Dashboard page updated to use canonical `useGetXxx` hook names, local `Plan` type from `../types`, and `analytics.dailyRevenue` as a number.",
  "frontend/src/pages/InventoryPage.tsx": "Inventory page updated to use `useGetInventoryState`/`useGetIngredients`, local `Ingredient` type, and updated component props (`onSuccess` instead of `onClose`, no `onEdit` on `IngredientList`).",
  "frontend/src/components/IngredientForm.tsx": "Form component for adding and editing ingredients with proper data transformation mapping form fields (name, quantity, unit, costPrice, supplier, lowStockThreshold) to backend IngredientInput format, converting unit dropdown values to Variant_kilograms_grams enum and parsing numeric values as Float.",
  "frontend/src/components/IngredientList.tsx": "Grid display of ingredient cards showing quantity, cost, supplier, threshold, and low stock badges with edit functionality, using number type for Weight.value and formatting quantities with two decimal places.",
  "frontend/src/pages/MenuPage.tsx": "Menu page updated to use `useGetRecipes`, local `Recipe` type, and updated `RecipeForm`/`RecipeList` props (`onSuccess` instead of `onClose`, no `onEdit` on `RecipeList`).",
  "frontend/src/components/RecipeForm.tsx": "Form component for creating recipes with name, bowl size selection, and dynamic ingredient list with quantities in grams, using parseFloat to convert quantity values to number type for backend compatibility.",
  "frontend/src/components/RecipeList.tsx": "Grid display of recipe cards showing bowl size, ingredients with quantities, and salad bowl icon with edit functionality.",
  "frontend/src/pages/SalesPage.tsx": "Sales page updated to use `useGetInvoices`/`useGetCustomers` and passes required `invoices`/`customers` props to `InvoiceList`, uses `onSuccess` for `InvoiceForm`.",
  "frontend/src/pages/SubscriptionsPage.tsx": "Subscription management page updated to use SubscriptionAdvanced type throughout, with stats cards, add/edit dialog with SubscriptionForm, and SubscriptionList display.",
  "frontend/src/pages/CustomersPage.tsx": "Customers page updated to use `useGetCustomers`, local `Customer` type, and `onSuccess` prop for `CustomerForm`.",
  "frontend/src/components/NotificationPanel.tsx": "Popover notification panel showing low-stock and expiring-subscription alerts, exported as both named `{ NotificationPanel }` and default export.",
  "frontend/src/hooks/useNotifications.ts": "Notification hook that compares customer IDs using `Number(c.id) === subscription.customerId` to fix bigint/number type mismatch, and removes the unused `useGetPlans` import.",
  "frontend/src/utils/csvExport.ts": "Utility function for exporting data to CSV format with proper escaping and date-stamped filenames.",
  "frontend/src/index.css": "Global styles with OKLCH color system using green (#22C55E equivalent) as primary color for light and dark modes.",
  "frontend/tailwind.config.js": "Tailwind configuration with green primary color, Inter font family, and custom design tokens for the Salad Khatora brand.",
  "frontend/src/components/CustomerForm.tsx": "Form component for adding and editing customers with fields for name, mobile (10-digit validation), email, reference source, preferences, and address using react-hook-form and JSON-encoded contactInfo storage.",
  "frontend/src/components/CustomerList.tsx": "Enhanced customer list component displaying customer cards with contact information, purchase statistics (total orders and amount spent), and click-to-view-details functionality opening CustomerDetailsDialog.",
  "frontend/src/components/SubscriptionForm.tsx": "Form for adding/editing subscriptions with plan selection dropdown (fetches from Plans tab), auto-fills bowl size and price from selected plan, auto-calculates read-only end date by counting delivery days (6 for weekly, 24 for monthly) skipping Sundays.",
  "frontend/src/components/SubscriptionList.tsx": "Grid of subscription cards that always computes the end date client-side via calculateEndDate (24 days, skipping Sundays) instead of relying on the backend stub value, displays the correct \"startDate → endDate\" period, shows delivery progress summary, and renders a collapsible DeliveryDayList for each subscription.",
  "frontend/src/hooks/useSalesAnalytics.ts": "Sales analytics hook updated to use `LocalInvoice` type with `number` recipe IDs throughout, eliminating all bigint/number type mismatches.",
  "frontend/src/components/InvoiceForm.tsx": "Simplified invoice form using local types, `useGetXxx` hooks, `onSuccess` callback, and a simple search-based customer selector without external combobox dependencies.",
  "frontend/src/components/InvoiceList.tsx": "Component displaying all sales invoices in a filterable list with search by invoice number or customer name, payment mode filter, customer type filter (subscription/regular), and click-to-view-details functionality.",
  "frontend/src/components/InvoiceDetailsDialog.tsx": "Dialog showing invoice details, updated to use `LocalInvoice` type with `number` IDs instead of `bigint`, fixing all type comparison errors.",
  "frontend/src/components/CustomerDetailsDialog.tsx": "Dialog showing customer details and purchase history, now uses `useGetInvoices` and filters by `Number(customer.id)` instead of the removed `useGetInvoicesByCustomer` hook.",
  "frontend/src/components/SalesAnalyticsCards.tsx": "Analytics cards updated to use `analytics.dailyRevenue` as a number, `analytics.repeatCustomers`, and `mode.percentage` from the updated `useSalesAnalytics` hook.",
  "frontend/src/components/RevenueChart.tsx": "Revenue chart updated to use `analytics.dailyTrend` and `analytics.monthlyTrend` from the updated `useSalesAnalytics` hook (no arguments needed).",
  "frontend/src/components/InventoryAdjustmentHistory.tsx": "Simplified placeholder component that shows an informational message since inventory adjustment backend endpoints no longer exist.",
  "frontend/src/components/PlanForm.tsx": "Form component for creating and editing plans with fields for plan name, plan price (renamed from \"Overall Plan Price\"), a bowl size dropdown (250gm/350gm/500gm selector), and a single price input for the selected bowl size — replacing the previous three separate bowl size price inputs.",
  "frontend/src/components/PlanList.tsx": "Grid display of plan cards showing plan name, plan price, a single bowl size with its price (replacing the old three-column bowl size pricing grid), and live enrollment count badges with edit and delete functionality.",
  "frontend/src/pages/PlansPage.tsx": "Plans page updated to use local `Plan` type, `useGetPlans`, and updated `PlanForm`/`PlanList` props (no `enrollmentCounts` prop, `onSuccess` instead of `onClose`).",
  "frontend/src/utils/dateCalculations.ts": "Utility module with functions for subscription date calculations — calculateEndDate (counts delivery days skipping Sundays), generateDeliveryDays (lists all non-Sunday days in a range), addOneDeliveryDay, subtractOneDeliveryDay, formatDate, formatDisplayDate, and getTodayStr.",
  "frontend/src/components/DeliveryDayList.tsx": "Delivery day tracking component that computes the end date via `calculateEndDate` then passes both date strings to `generateDeliveryDays`, fixing the number-vs-string argument error.",
  "frontend/src/types.ts": "Type re-exports file updated to include `InventoryAdjustment` and `InventoryState` stub types so existing components that import them don't break."
}